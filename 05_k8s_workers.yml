---
- name: Setup Kubernetes Worker Nodes
  hosts: workers
  become: yes
  vars:
    harbor_ip: "{{ hostvars[groups['harbor'][0]]['ansible_host'] }}"
    monitor_ip: "{{ hostvars[groups['monitoring'][0]]['ansible_host'] }}"
    k8s_version: "1.30"
    # master API endpoint (HAProxy VIP)
    api_endpoint: "10.1.1.10"

  tasks:
    # ---------------------------------------------------------------
    # STEP 1. 커널 및 네트워크 사전 준비
    # ---------------------------------------------------------------
    - name: 1. 커널 모듈 로드 (overlay, br_netfilter)
      ansible.builtin.command: modprobe {{ item }}
      loop:
        - overlay
        - br_netfilter

    - name: 2. 커널 모듈 부팅 시 자동 로드 설정
      copy:
        dest: /etc/modules-load.d/k8s.conf
        content: |
          overlay
          br_netfilter

    - name: 3. sysctl 네트워크 설정 (Persistence 포함)
      ansible.builtin.command: sysctl -w {{ item.name }}={{ item.value }}
      loop:
        - { name: 'net.bridge.bridge-nf-call-iptables',  value: '1' }
        - { name: 'net.bridge.bridge-nf-call-ip6tables', value: '1' }
        - { name: 'net.ipv4.ip_forward',                 value: '1' }

    # ---------------------------------------------------------------
    # STEP 2. Containerd 설치 및 설정
    # ---------------------------------------------------------------
    - name: 4. Containerd 설치
      apt:
        name: containerd.io
        state: present
        update_cache: yes

    - name: "5. Containerd 설정 파일 생성 (2.x 호환)"
      ansible.builtin.copy:
        dest: /tmp/fix_containerd.py
        mode: "0755"
        content: |
          import re
          with open("/etc/containerd/config.toml", "r") as f:
              c = f.read()
          c = c.replace("SystemdCgroup = false", "SystemdCgroup = true")
          c = re.sub(r"disabled_plugins\s*=\s*\[[^\]]*\"cri\"[^\]]*\]", "disabled_plugins = []", c)
          c = re.sub(r'sandbox_image\s*=\s*"[^"]*"', 'sandbox_image = "{{ harbor_domain }}/{{ harbor_project }}/pause:3.9"', c)
          c = re.sub(r"sandbox\s*=\s*'[^']*'", "sandbox = '{{ harbor_domain }}/{{ harbor_project }}/pause:3.9'", c)
          c = re.sub(r"^[ \t]*config_path[ \t]*=[^\n]*\n", "", c, flags=re.MULTILINE)
          if "io.containerd.cri.v1.images" in c:
              c = re.sub(
                  r"(\[plugins\.'io\.containerd\.cri\.v1\.images'\.registry\])",
                  r"\1\n        config_path = '/etc/containerd/certs.d'",
                  c
              )
          else:
              c = re.sub(
                  r'(\[plugins\."io\.containerd\.grpc\.v1\.cri"\.registry\])',
                  r'\1\n        config_path = "/etc/containerd/certs.d"',
                  c
              )
          with open("/etc/containerd/config.toml", "w") as f:
              f.write(c)
          print("config.toml updated")

    - name: "5-b. Containerd config.toml 생성 및 최적화 실행"
      ansible.builtin.shell: |
        set -e
        mkdir -p /etc/containerd
        [ -f /etc/containerd/config.toml ] || containerd config default > /etc/containerd/config.toml
        python3 /tmp/fix_containerd.py
      changed_when: true

    - name: 7. Containerd 즉시 재시작 및 활성화
      ansible.builtin.systemd:
        name: containerd
        state: restarted
        enabled: yes
        daemon_reload: yes

    - name: 8. Containerd 소켓 준비 대기
      wait_for:
        path: /run/containerd/containerd.sock
        state: present
        timeout: 30

    # ---------------------------------------------------------------
    # STEP 3. Kubernetes 패키지 설치
    # ---------------------------------------------------------------
    - name: 9. K8s 로컬 저장소 확인 (A4에서 등록됐어야 함)
      ansible.builtin.stat:
        path: /etc/apt/sources.list.d/miso-local-k8s.list
      register: k8s_repo_check

    - name: 10. K8s 로컬 저장소 긴급 등록 (미등록 시)
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/miso-local-k8s.list
        content: |
          deb [trusted=yes] http://{{ harbor_ip }}:8080/debs/k8s ./
      when: not k8s_repo_check.stat.exists

    - name: 11. 외부 K8s 저장소 비활성화 (폐쇄망)
      ansible.builtin.file:
        path: /etc/apt/sources.list.d/kubernetes.list
        state: absent

    - name: 12. kubelet / kubeadm / kubectl 설치
      apt:
        name:
          - kubelet
          - kubeadm
          - kubectl
        state: present
        update_cache: yes

    - name: 13. kubelet / kubeadm / kubectl 버전 고정 (apt-mark hold)
      dpkg_selections:
        name: "{{ item }}"
        selection: hold
      loop:
        - kubelet
        - kubeadm
        - kubectl

    - name: 14. kubelet 활성화 (시작은 join 후 자동)
      ansible.builtin.systemd:
        name: kubelet
        enabled: yes
        daemon_reload: yes

    # ---------------------------------------------------------------
    # STEP 4. Worker Join (토큰은 master1에서 발급)
    # ---------------------------------------------------------------
    - name: 15. (run_once) worker join 커맨드 발급 (master1에서 실행)
      delegate_to: master1
      run_once: true
      become: yes
      command: kubeadm token create --print-join-command
      register: worker_join_cmd

    - name: 16. worker kubeadm join 실행
      command: "{{ worker_join_cmd.stdout }}"
      args:
        creates: /etc/kubernetes/kubelet.conf

    # ---------------------------------------------------------------
    # STEP 5. (선택) Bastion에서 조인 결과 확인
    #  - bastion에서 kubectl을 쓸 수 있게 /tmp/kubeconfig-admin.conf가 있어야 합니다.
    #  - 없으면 이 블록은 무시해도 됩니다.
    # ---------------------------------------------------------------
    - name: 99. (run_once) bastion에서 노드 상태 확인 (가능한 경우)
      delegate_to: localhost
      run_once: true
      become: false
      environment:
        KUBECONFIG: /tmp/kubeconfig-admin.conf
      shell: |
        kubectl get nodes -o wide
      register: nodes_out
      changed_when: false
      failed_when: false

    - name: 99-b. (run_once) 노드 출력
      delegate_to: localhost
      run_once: true
      debug:
        var: nodes_out.stdout_lines
      when: nodes_out is defined